# C/C++ build artifacts of the generated client library.
# The generated output itself (example/lib/, produced by example/run-compile.sh) is already
# covered by the repository root .gitignore, which ignores lib/ everywhere.

# CMake build tree
build/
CMakeCache.txt
CMakeFiles/
cmake_install.cmake

# Compiled objects, archives and shared objects
*.o
*.a
*.so
*.dylib

.vscode
.idea
